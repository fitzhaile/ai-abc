# Bulky raw HTML mirrors (182 MB + 25 MB) are NOT versioned: they are
# re-fetchable evidence, regenerated by the scrape/ scripts. Everything
# derived FROM them (data/interactive/*.json, data/extracted/, dashboard
# data) IS versioned, as is the crawl index that records what was fetched.
data/raw/
data/interactive/club_details_html/

# Playwright MCP session artifacts (snapshots, console logs)
.playwright-mcp/

# Python bytecode
__pycache__/
*.pyc

# macOS
.DS_Store
